# 2026-05-18

## Сессия 1 (старт)
Тема: открытие сессии

### Сделано
- Прочитал SOUL.md, MEMORY.md, GOALS.md, memory/README.md
- Создал дневник за сегодня

## Сессия 2 — Дизайн парсера Telegram-чатов
Тема: brainstorming парсера участников групповых чатов

### Сделано
- Прошли brainstorming по всем 6 секциям дизайна (архитектура, файлы, REST API, веб-flow, бот-flow, безопасность)
- Изучил код существующего бота `bot/index.js` — паттерн модулей через `registerXxxHandlers(bot)`
- Спека опубликована: `docs/superpowers/specs/2026-05-18-telegram-parser-design.md` (316 строк, commit 8a1b2dd)
- Диагностика бота: бот не отвечал → задача `GideonBot` в Планировщике не была запущена → стартанул задачу руками, бот заработал (PID 9096)
- Поправил MEMORY.md: бот запускается через `start-bot.bat` (не PM2), автозапуск через Планировщик Windows
- Поправил MEMORY.md: имя бота `@flash_gideon_bot` (раньше было `@GideonBot`)

### Решения по парсеру
- Стек: Node.js + GramJS + Express + ванильный HTML/CSS/JS
- Парсинг через User API (MTProto), не Bot API
- Только групповые чаты до 10К участников, только `@username`
- Две точки входа: веб (`http://VPS:3000?token=...`) и бот (`/parser` + кнопка «Открыть в браузере»)
- Бот ходит в парсер по HTTP на `localhost:3000`, без импортов между папками
- API_ID/API_HASH — только ручным редактированием `parser/.env` (без веб-формы для безопасности)
- Telegram-авторизация через веб-форму (телефон → код → 2FA), session в `parser/data/session.txt`

### TODO на потом
- Александру: revoke токена бота в @BotFather (старый засветился в переписке)
- После revoke: положить новый токен в `~/.agent/.env`, перезапустить задачу GideonBot

## Сессия 3 — Реализация парсера (subagent-driven)
Тема: исполнение плана 2026-05-18-telegram-parser.md

### Сделано
- Все 22 кодовые задачи реализованы через subagent-driven-development (haiku для механического, sonnet для интеграционного)
- 22 коммита на main с русскими сообщениями
- 34/34 unit + integration тестов через node:test (chatref, session, chats-filter, server)
- Расширение по запросу Александра: добавлен auto-join по invite-ссылке + автоматический leave после парсинга
- Изменения в `lib/telegram.js` (`joinByInvite`, `leaveChat`, новый сигнатур `resolveChat`)
- Изменения в `server.js` `/api/parse` — после парсинга если joinedNow=true, выходим из чата в finally блоке
- Обновлены подсказки в UI (`public/index.html`) и боте (`bot/parser-menu.js`) — описаны 3 формата ссылок
- CLAUDE.md правка: read-only зона `~/.agent/bot/` теперь означает «продакшн копия, менять в gideon/bot/ → синхронизация»
- MEMORY.md обновлён: парсер записан в Активные проекты и Инфраструктура

### Решения
- Auto-join по invite — только если ссылка типа `t.me/+xxx` или `t.me/joinchat/xxx`. Для `@username` парсим напрямую без вступления.
- При `INVITE_REQUEST_SENT` (одобрение админа требуется) возвращаем 202 + подсказка повторить позже
- leaveChat сначала пробует `LeaveChannel` (для супергрупп), потом `DeleteChatUser` (для legacy basic groups)
- Александр осознанно принял риск анти-спам бана Telegram за auto-join+leave — это серая зона

## Сессия 4 — Деплой парсера и доработки UX

### Сделано
- my.telegram.org с VPS-IP не отдал API_ID (известная блокировка датацентров) — взяли публичные ключи Telegram Desktop (api_id=17349) как временное решение
- `parser/.env` создан с публичными ключами, парсер запущен под PM2 (`agent-parser`, PID живёт)
- Александр прошёл веб-авторизацию через `http://138.16.178.94:3000?token=<AUTH_TOKEN>`, session сохранилась в `parser/data/session.txt`
- `PARSER_AUTH_TOKEN` добавлен в `~/.agent/.env` через PowerShell Add-Content (без чтения остального файла)
- `bot/index.js` и `bot/parser-menu.js` синхронизированы в `~/.agent/bot/`, бот перезапущен через `pm2 restart agent-bot`
- E2E проверено: `/parser` в @flash_gideon_bot работает, парсинг публичных чатов и приватных по invite — успешный
- Один раз поймали `SESSION_REVOKED` после `pm2 restart agent-parser` — стёрли `session.txt`, переавторизовались, дальше работало
- `pm2 save` — состояние PM2 (бот + парсер) персистенто, выживет перезагрузку VPS

### Доработки UX по запросу Александра
- **Нумерация** в .txt и в сообщении бота (1. 2. 3. ...)
- **Отправка списка текстовыми сообщениями в чат бота** (после .txt) — список режется на куски по 3900 символов по границе строк
- **Пометки ролей**: `🤖` бот, `👑` админ, `⭐` создатель чата (для админов потребовалось 3 итерации фикса: BigInt id → admins не в основном списке участников → добавляем их в конец → сортировка по роли)
- **Сортировка по роли**: сначала creator, потом админы (включая бот-админов), потом боты, потом обычные
- **Отдельный блок «Админы»** в сводке бота с явным списком username'ов
- **Человеческое сообщение при session_revoked**: вместо «Ошибка: session_revoked» — инструкция «Открой веб-парсер и пройди авторизацию заново»

### Решения / тех. долг
- Публичные ключи Telegram Desktop — временное решение. Минусы: Telegram периодически отзывает session если параллельно работает реальный TG Desktop с тем же аккаунтом, плюс серая зона ToS. План: создать свои api_id с домашнего компьютера/телефона (не VPS) когда будет время
- В чате «Вайбкодинг 2.0» админы — только два бота (`@NB360AdminBot`, `@joinhide21_bot`). Людей-админов нет — анонимный режим админов Telegram скрывает их даже от списка `ChannelParticipantsAdmins`. Это особенность чата, не парсера
- Тесты `parser/test/server.test.js` после боевой авторизации могут флапать (стейл-session мешает фейковым API_ID=1 в тестах). Тех. долг: тесты должны использовать temp dir для session.txt

### Состояние сервисов (закрытие спринта)
- `agent-bot` — online под PM2, токен в `~/.agent/.env`, код в `~/.agent/bot/`, синхронизирован с шаблоном в `gideon/bot/`
- `agent-parser` — online под PM2, порт 3000, session в `parser/data/session.txt`, AUTH_TOKEN в `parser/.env`
- Доступ к веб-парсеру: `http://<VPS_IP>:3000/?token=<AUTH_TOKEN>` (токен — в `parser/.env`, NOT committed)
- Команда в боте: `/parser` → меню → выбор чата → .txt + сообщение со списком

### TODO на потом
- Создать свои api_id/api_hash на my.telegram.org с домашнего ПК или телефона, заменить публичные в `parser/.env`
- Починить тесты `server.test.js` — изолировать от боевого `data/session.txt` через temp dir
- При желании: HTTPS для веб-парсера (Cloudflare Tunnel или DuckDNS+Let's Encrypt), чтобы можно было добавить Menu-кнопку Telegram WebApp в бота
